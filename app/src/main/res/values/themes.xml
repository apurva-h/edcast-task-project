<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DemoApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="textViewBoldTheme">
        <item name="android:paddingStart">@dimen/dimen_8dp</item>
        <item name="android:textColor">@color/colorLable</item>
        <item name="android:textSize">@dimen/dimen_18dp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="textViewLightTheme">
        <item name="android:paddingStart">@dimen/dimen_8dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/dimen_18dp</item>
        <item name="android:textStyle">normal</item>
    </style>
</resources>